// Tạo lớp đối tượng user3
function User(id, name, image, language, type, mota) {
  // Thuộc tính (Properties)
  this.id = id;
  this.name = name;
  this.image = image;
  this.language = language;
  this.type = type;
  this.mota = mota;
}
