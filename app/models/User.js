// Tạo lớp đối tượng user3
function User(id, tk, mk, name, email, language, type, image) {
  // Thuộc tính (Properties)
  this.id = id;
  this.tk = tk;
  this.mk = mk;
  this.name = name;
  this.email = email;
  this.language = language;
  this.type = type;
  this.image = image;
}
